name: Azure Static Web Apps CI/CD (QA)

on:
  push:
    branches:
      - qa
    paths:
      - "api/**"
      - "src/**"
      - "index.html"
      - "package.json"
      - "vite.config.*"
      - ".github/workflows/azure-static-web-apps-victorious-river-07a302f1e.yml"
  pull_request:
    types: [opened, synchronize, reopened, closed]
    branches:
      - qa

jobs:
  build_and_deploy_job:
    if: github.event_name == 'push' || (github.event_name == 'pull_request' && github.event.action != 'closed')
    runs-on: ubuntu-latest
    name: Build and Deploy Job
    steps:
      - uses: actions/checkout@v4

      - name: Use Node.js 20
        uses: actions/setup-node@v3
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm ci

      - name: Build Vite app
        run: npm run build

      - name: Deploy to Azure Static Web Apps
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN_VICTORIOUS_RIVER_07A302F1E }}
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          action: "upload"
          app_location: "dist"         # ⚠️ ya está compilado
          output_location: ""          # ⚠️ no volver a compilar
          api_location: "api"          # Azure Functions, si usás

  close_pull_request_job:
    if: github.event_name == 'pull_request' && github.event.action == 'closed'
    runs-on: ubuntu-latest
    name: Close Pull Request Job
    steps:
      - name: Close Pull Request
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN_VICTORIOUS_RIVER_07A302F1E }}
          action: "close"
